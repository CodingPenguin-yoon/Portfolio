services:
  portfolio:
    image: yunho-portfolio:local
    build:
      context: .
      target: runtime
    ports:
      - '${PORT:-8088}:8080'
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
    security_opt:
      - no-new-privileges:true
